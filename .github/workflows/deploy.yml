name: Deploy Workflow

on:
 workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Read content from deploy.sql and update change.log
      run: |
        './deploy.sh'
        
